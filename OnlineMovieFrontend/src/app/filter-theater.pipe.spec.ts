import { FilterTheaterPipe } from './filter-theater.pipe';

describe('FilterTheaterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTheaterPipe();
    expect(pipe).toBeTruthy();
  });
});
